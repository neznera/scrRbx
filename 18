local remote = game:GetService("ReplicatedStorage").Events.PlayerPurchase
if remote then
    remote:FireServer("Treat", 100000000)
    print("✅ Запрос PlayerPurchase отправлен ('Treat', 100000000)")
end
